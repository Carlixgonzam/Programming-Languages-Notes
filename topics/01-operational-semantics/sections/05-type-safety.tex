% !TeX root = ../main.tex
\section{Type safety}
% Sugerencias de contenido:
%   - Enunciado de progreso (\emph{progress}):
%     si $\vdash e : \tau$ entonces $e$ es un valor o existe $e'$ con $e \reduces e'$.
%   - Enunciado de preservación (\emph{preservation}):
%     si $\vdash e : \tau$ y $e \reduces e'$ entonces $\vdash e' : \tau$.
%   - Lema de canonicidad de valores
%   - Lema de sustitución
%   - Esquema de la demostración por inducción estructural
